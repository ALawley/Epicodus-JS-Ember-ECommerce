import { totalDisplay } from 'delivery/helpers/total-display';
import { module, test } from 'qunit';

module('Unit | Helper | total display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalDisplay([42]);
  assert.ok(result);
});
