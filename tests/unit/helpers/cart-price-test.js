import { cartPrice } from 'delivery/helpers/cart-price';
import { module, test } from 'qunit';

module('Unit | Helper | cart price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartPrice([42]);
  assert.ok(result);
});
